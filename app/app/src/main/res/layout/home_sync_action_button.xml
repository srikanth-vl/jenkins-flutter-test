<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:id="@+id/home_sync_parent_layout"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/refresh_image_to_rotate"
        style="@android:style/Widget.ActionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:adjustViewBounds="true"
        android:contentDescription="@string/app_name"
        android:paddingBottom="10dp"
        android:paddingEnd="10dp"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:paddingStart="10dp"
        android:paddingTop="10dp"
        android:layout_marginLeft="@dimen/dimen_5dp"
        android:layout_marginRight="5dp"
        android:layout_marginBottom="2.5dp"
        android:layout_marginTop="5dp"
        android:scaleType="fitCenter"
        android:src="@drawable/action_bar_refresh" />

    <TextView
        android:id="@+id/refresh_image_unsync_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/text_size_14"
        android:textStyle="bold"
        android:layout_alignEnd="@+id/refresh_image_to_rotate"
        android:layout_alignRight="@+id/refresh_image_to_rotate"
        android:layout_alignBottom="@+id/refresh_image_to_rotate"
        android:textColor="@color/white" />

    <!--<TextView-->
        <!--android:id="@+id/refresh_image_sync_time_info"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:layout_alignBottom="@+id/refresh_image_to_rotate"-->
        <!--android:layout_alignEnd="@+id/refresh_image_to_rotate"-->
        <!--android:layout_alignRight="@+id/refresh_image_to_rotate"-->
        <!--android:textColor="@color/white" />-->

</RelativeLayout>